---
import Badge from '../components/Badge.astro';
import SocialPill from '../components/SocialPill.astro';

import SparklesIcon from '../components/icons/Sparkles.astro'
import LinkedinIcon from '../components/icons/Linkedin.astro';
import GitHubIcon from '../components/icons/GitHub.astro';
import MailIcon from '../components/icons/Mail.astro';

import { basics } from '@cv'
---

<div class="max-w-xl">
    <div class="anim-fade-in flex gap-4 mb-6 items-center">
        <!-- Avatar with ambient glow -->
        <div class="relative shrink-0">
            <div
                class="absolute inset-0 rounded-full bg-yellow-400/30 dark:bg-yellow-400/20 blur-xl scale-125"
                aria-hidden="true"
            >
            </div>
            <img 
                class="relative rounded-full shadow-2xl size-20 ring-2 ring-yellow-400/60 ring-offset-2 ring-offset-gray-50 
                    dark:ring-offset-gray-950"
                src={basics.image}
                alt={basics.name}
            />
        </div>
        
        <a
            href={basics.profiles[2].url}
            target="_blank"
            rel="noopener noreferrer"
            class="flex items-center transition md:justify-center md:hover:scale-105"
        >
            <Badge><SparklesIcon class="size-3" />Disponible para trabajar</Badge>
        </a>
    </div>
    
    <h1 class="anim-fade-in anim-delay-1 text-4xl font-bold tracking-tight text-gray-800 sm:text-5xl dark:text-white">
        Hola, soy <span class="gradient-name">Santi</span>
    </h1>

    <p class="anim-fade-in anim-delay-2 mt-6 text-xl text-gray-800 dark:[&>strong]:text-yellow-200 
            [&>strong]:text-yellow-500 [&>strong]:font-semibold dark:text-gray-300">
        <strong>Ingeniero Informático. </strong> De A Coruña, Galicia. Recién graduado en Ingeniería Informática en la <strong>UDC</strong> y en busca de comenzar mi carrera profesional.
    </p>

    <nav
        class="anim-fade-in anim-delay-3 flex flex-wrap gap-4 mt-8"
        aria-label="Redes sociales y contacto"
    >
        <SocialPill href={`mailto:${basics.profiles[0].username}`}>
            <MailIcon class="size-4"/>
            Contáctame
        </SocialPill>
        <SocialPill href={basics.profiles[2].url}>
            <LinkedinIcon class="size-4"/>
            LinkedIn
        </SocialPill>
        <SocialPill href={basics.profiles[1].url}>
            <GitHubIcon class="size-4"/>
            GitHub
        </SocialPill>
    </nav>

</div>